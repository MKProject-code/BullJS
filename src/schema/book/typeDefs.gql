####################################
type Book {
    id: ID!
    author: User
    title: String!
}

####################################
extend type Query {
    book(id: ID!): Book
    books: [Book]
}

####################################